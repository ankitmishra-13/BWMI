# Optional. Without this key, the step explainer uses its deterministic bilingual fallback.
OPENAI_API_KEY=

# Required for a deployed public demo. Use a long random value; never expose it to the browser.
DEMO_SESSION_SECRET=
